// Primero, decido recorrer los números del 1 al 20
for (let i = 1; i <= 20; i++) {


    // Luego, verifico si el número actual es múltiplo de 3
    if (i % 3 === 0) {

        // Si lo es, lo muestro en pantalla como resultado
        console.log(i);
    }

    // Si no lo es, simplemente paso al siguiente número
}